function bestShift_n = bestCircularShift(x,y)

    x = x(:)-mean(x(:));
    y = y(:)-mean(y(:));
    n = length(x);
    if length(y) ~= n
        error('Must be the same length');
    end
    dots = zeros(1,n);
    for i = 1:n
        dots = 
        
    end
    


end   